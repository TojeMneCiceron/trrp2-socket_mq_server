consoleRssServer {
    address = "http://localhost:8080/"
}

messageQueueServer {
    host = "localhost"
    port = 5672
    username = "guest"
    password = "guest"
}

cacheServer {
    host = 25.46.195.40
    port = 32948
}